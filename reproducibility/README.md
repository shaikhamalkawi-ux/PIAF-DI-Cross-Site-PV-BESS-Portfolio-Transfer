# Reproducibility

This directory will contain public-safe reproducibility code, manifests, and verification instructions after the R6R8 clean-room replay is reviewed.

Large/raw/controlled data remain in the authenticated Google Drive workspace until licence and release checks are complete.

Do not claim a PASS for a replay step when the required machine-readable input is absent. Use HOLD instead.
